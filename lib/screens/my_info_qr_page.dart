import 'package:flutter/material.dart';

class MyInfoQRPage extends StatelessWidget {
  const MyInfoQRPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
